package org.example.command;

import org.example.command.Command;
import org.example.ds.TreeNode;

public class UpCommand implements Command {
    public UpCommand(TreeNode curr) {
    }

    @Override
    public void execute() {

    }
}
